{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@ant-design/icons": "^5.0.1",
    "dumi": "^2.2.1",
    "father-build": "^1.22.5",
    "lerna": "^6.6.2",
    "react": "^17.0.2"
  },
  "scripts": {
    "start": "dumi dev",
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean",
    "bootstrap:china": "lerna bootstrap --registry=https://registry.npmmirror.com",
    "build": "father-build",
    "icon:build": "lerna run --scope iconfont-extract-icon build",
    "icons:build": "lerna run --scope iconfont-extract build",
    "lint": "lerna run lint"
  },
  "dependencies": {
    "-": "0.0.1",
    "@types/node": "^20.1.0",
    "D": "^1.0.0"
  }
}
